import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
<% if (data.useTailwind) { %>import tailwindcss from '@tailwindcss/vite';<%="\n"%><% } else { %><% } %>
// https://vite.dev/config/
export default defineConfig({
    <% if (data.useTailwind) { %>plugins: [react(), tailwindcss()],<% } else { %>plugins: [react()],<% } %>
    resolve: {
        alias: {
            '@': '/src',
        },
    },
});
